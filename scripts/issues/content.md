Inline style attribute
HTML elements should use a .css file for styling, making your HTML cleaner and more maintainable.
3
40
Content meets length requirements
Page's content should contain more than 150 words.

-
- All CSS files are smaller than 300 KB
  Avoid using large .css files which slow down page load time and hurt performance. CSS files should be smaller than 300 KB.
-
- No .doc files linked on page
  Instead of linking to a .doc or .docx file, try to use a .pdf instead. PDFs are compatible across all operating systems.
-
- No empty href attributes in links found
  All links on a page should have an explicit "href" attribute defined to avoid cross-browser compatibility issues and ensure search engines can discover the linked-to pages.
-
- No meta refresh redirect
  Use of the meta refresh tag is no longer considered best practice by search engines.
-
- HTML lang attribute present
  Having a lang tag can help search engines better classify your webpages in regional search results.
-
- All JS files are smaller than 300 KB
  Avoid using large javascript files, which slow down page load time and hurt performance. Files should be smaller than 300 KB.
-
- Page DOM size is smaller than 1000 elements
  Large DOM sizes hurt performance by making it harder for browsers to render a webpage. Your Page should have a DOM size of less than 1000 elements.
-
- Meta keywords present
  Page can implement <meta> keywords tag. This is optional.
-
- No bgsound tag detected
  The bgsound tag is no longer considered best practice and may hurt your page's ability to rank i
